<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper
  PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
  "http://mybatis.org/dtd/mybatis-3-mapper.dtd">
<mapper namespace="com.ssafy.passproject.model.mapper.NoticeMapper">

    <!-- 공지사항 추가 -->
    <insert id="insertNotice" parameterType="NoticeDto">
        INSERT INTO notice (title, member_id, content)
        VALUES (#{title}, #{memberId}, #{content})
    </insert>

    <!-- 공지사항 수정 -->
    <update id="updateNotice" parameterType="NoticeDto">
        UPDATE notice
        SET title = #{title}, content = #{content}
        WHERE id = #{id}
    </update>

    <!-- 공지사항 삭제 -->
    <delete id="deleteNotice" parameterType="int">
        DELETE FROM notice WHERE id = #{id}
    </delete>

    <!-- 특정 공지사항 조회 -->
    <select id="selectNoticeById" parameterType="int" resultType="NoticeDto">
        SELECT id, member_id as memberId, title, content, created_date AS createdDate
        FROM notice
        WHERE id = #{id}
    </select>

    <!-- 모든 공지사항 조회 -->
    <select id="selectAllNotices" resultType="NoticeDto">
        SELECT id, member_id as memberId, title, content, created_date AS createdDate
        FROM notice
        ORDER BY created_date DESC
    </select>

    <!-- 공지사항 검색 -->
    <select id="searchNotices" parameterType="string" resultType="NoticeDto">
        SELECT id, member_id as memberId, title, content, created_date AS createdDate
        FROM notice
        WHERE title LIKE CONCAT('%', #{keyword}, '%')
           OR content LIKE CONCAT('%', #{keyword}, '%')
        ORDER BY created_date DESC
    </select>

</mapper>
