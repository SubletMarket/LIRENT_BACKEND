<?xml version="1.0" encoding="UTF-8" ?>
<!DOCTYPE mapper PUBLIC "-//mybatis.org//DTD Mapper 3.0//EN"
        "http://mybatis.org/dtd/mybatis-3-mapper.dtd">

<mapper namespace="com.ssafy.lirent.model.mapper.MemberMapper">
    <resultMap id="member" type="MemberDto">
        <result column="member_id" property="memberId" />
        <result column="email" property="email" />
        <result column="password" property="password" />
        <result column="phone" property="phone" />
        <result column="address" property="address" />
        <result column="nickname" property="nickname" />
        <result column="created" property="created" />
    </resultMap>

    <select id="getMemberById" parameterType="String" resultMap="member">
        SELECT member_id, email, password, phone, address, nickname, created
        FROM members
        WHERE email = #{email} AND password = #{password}
    </select>

    <select id="login" parameterType="String" resultType="int">
        SELECT member_id
        FROM members
        WHERE email = #{email} AND password = #{password}
    </select>

    <insert id="regist" parameterType="memberDto">
        INSERT INTO members(email, password, phone, address, nickname)
        VALUES (#{email}, #{password}, #{phone}, #{address}, #{nickname});
    </insert>

    <update id="update" parameterType="memberDto">
        UPDATE members
        SET email=#{email},
            password=#{password},
            phone=#{phone},
            address=#{address},
            nickname=#{nickname}
        WHERE member_id=#{memberId}
    </update>

    <delete id="delete" parameterType="int">
        DELETE FROM members
        WHERE member_id=#{memberId}
    </delete>
</mapper>